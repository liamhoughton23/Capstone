
@{
    ViewBag.Title = "Events";
}

<h2>Events</h2>

<div class="row">
    <div class="col-md-3">
        <input type="button" value="Schedule Game" onclick="location.href='@Url.Action("Create", "Event", new { id = Model.Id })'" class="btn btn-default" />
    </div>
</div>
<div class="row">
    <div class="col-md-3">
        <input type="button" value="Schedule Practice" onclick="location.href='@Url.Action("Create", "Event", new { id = Model.Id })'" class="btn btn-default" />
    </div>
</div>
<div class="row">
     <div class="col-md-3">
         <input type="button" value="Schedule Fundraiser" onclick="location.href='@Url.Action("Create", "Event", new { id = Model.Id })'" class="btn btn-default" />
      </div>
</div>
<div class="row">
    <div class="col-md-3">
        <input type="button" value="Schedule Meeting" onclick="location.href='@Url.Action("Create", "Event", new { id = Model.Id })'" class="btn btn-default" />
    </div>
</div>


